# tbx-lab-xhs 微信云托管部署步骤

## 一、服务定位

这个目录是独立服务：`tbx-lab-xhs`。

它只负责“小红书内容智能发布平台”，不要放进已有的 `tbx-lab-admin`。

## 二、在微信云托管新建服务

1. 进入微信云托管控制台。
2. 新建服务。
3. 服务名填写：

```text
tbx-lab-xhs
```

4. 部署方式选择代码仓库或上传代码包。
5. 构建方式使用本目录的 `Dockerfile`。

## 三、环境变量

在 `tbx-lab-xhs` 服务的环境变量里配置：

```env
LLM_PROVIDER=hunyuan
HUNYUAN_API_KEY=你的腾讯混元APIKey
HUNYUAN_MODEL=hunyuan-turbos-latest
HUNYUAN_BASE_URL=https://api.hunyuan.cloud.tencent.com/v1
ALLOW_LOCAL_FALLBACK=0
```

`ALLOW_LOCAL_FALLBACK=0` 表示正式环境里如果混元调用失败，就直接报错，避免员工误以为已经用上真实大模型。

## 四、访问地址

部署完成后访问服务默认域名。

打开根路径 `/` 就是小红书工作台：

```text
https://你的云托管域名/
```

健康检查：

```text
https://你的云托管域名/health
```

模型连通性检查：

```text
https://你的云托管域名/api/v1/xhs/model-test
```

如果返回：

```json
{"status":"ok","provider":"hunyuan"}
```

说明腾讯混元已经连通。

如果返回 `HUNYUAN_API_KEY 未配置`，说明环境变量没有配置到当前服务，或配置后没有重新发布。

如果返回 502 或混元接口错误，检查：

- `HUNYUAN_API_KEY` 是否属于已开通混元服务的腾讯云账号；
- `HUNYUAN_MODEL` 是否填写为 `hunyuan-turbos-latest`；
- `HUNYUAN_BASE_URL` 是否填写为 `https://api.hunyuan.cloud.tencent.com/v1`；
- 微信云托管环境变量是否使用了正确 JSON 格式。

## 五、不要改动的服务

已有服务 `tbx-lab-admin` 是小程序后台管理系统。

不要把小红书工作台页面合进去，也不要为了这个功能重启或覆盖 `tbx-lab-admin`。
